<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="xamtest.CustomViews.LanguagePicker"
    xmlns:i18n="clr-namespace:xamtest.Data;assembly=xamtest"
    xmlns:app="clr=namespace:xamtest.App;assembly=xamtest">
  <ContentView.Content>

        <StackLayout Orientation="Horizontal" Padding="15, 0" HeightRequest="40" x:Name="LangWrapper" x:Key="LanguagePickerLayout" BackgroundColor="Aqua">
            <Picker x:Name="LangPicker" SelectedIndex="{Binding SelectedCultureId, Mode=TwoWay}" Title="{Binding SelectedCultureId, {i18n:Translate LangChoice}, Mode=TwoWay}"  Scale=".6"  HorizontalOptions="Start" >
                <Picker.Items>
                    <x:String>English</x:String>
                    <x:String>Deutsch</x:String>
                    <x:String>Български</x:String>
                </Picker.Items>
            </Picker>
            <Image HorizontalOptions="EndAndExpand" VerticalOptions="Center" HeightRequest="25" x:Name="flag" Source="{Binding SelectedCulture.ImageSrc}">
            </Image>
        </StackLayout>



    </ContentView.Content>
</ContentView>
